export function ArchiveDialog({ title, archived, onClose, onConfirm }: { title: string; archived: boolean; onClose: () => void; onConfirm: () => void }) { return <div className="fixed inset-0 z-30 flex items-center justify-center bg-black/40 p-4" onClick={onClose}><div onClick={(e)=>e.stopPropagation()} className="w-full max-w-sm rounded-2xl border border-border bg-surface p-6"><h2 className="font-semibold text-ink">{archived?'Restore':'Archive'} {title}?</h2><p className="mt-2 text-sm text-ink-muted">{archived?'It will return to your library.':'It stays available in Archived and can be restored later.'}</p><div className="mt-5 flex justify-end gap-2"><button onClick={onClose} className="px-3 py-2 text-sm text-ink-muted">Cancel</button><button onClick={onConfirm} className="rounded-lg bg-navy px-4 py-2 text-sm text-bg">{archived?'Restore':'Archive'}</button></div></div></div> }
