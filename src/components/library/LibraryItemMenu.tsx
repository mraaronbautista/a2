export function LibraryItemMenu({ isFavorite, archived, onFavorite, onArchive, onMove, onDelete }: { isFavorite: boolean; archived: boolean; onFavorite: () => void; onArchive: () => void; onMove?: () => void; onDelete?: () => void }) {
  return <div className="flex shrink-0 items-center gap-1"><button type="button" onClick={onFavorite} className="rounded p-2 text-accent" aria-label={isFavorite ? 'Remove favorite' : 'Favorite'}>{isFavorite ? '★' : '☆'}</button>{onMove && !archived && <button type="button" onClick={onMove} className="rounded px-2 py-1 text-xs text-ink-muted hover:bg-bg">File</button>}<button type="button" onClick={onArchive} className="rounded px-2 py-1 text-xs text-ink-muted hover:bg-bg">{archived ? 'Restore' : 'Archive'}</button>{archived&&onDelete&&<button type="button" onClick={onDelete} className="rounded px-2 py-1 text-xs text-accent hover:bg-bg">Delete</button>}</div>
}
