export interface PagePreview { pageNumber: number; textPreview: string }
export function PageThumbnailRail({ pages, currentPage, onSelect }: { pages: PagePreview[]; currentPage: number; onSelect: (pageNumber: number) => void }) {
  return <nav aria-label="Document pages" className="flex gap-2 overflow-x-auto pb-2 lg:max-h-[calc(100dvh-12rem)] lg:w-36 lg:flex-col lg:overflow-y-auto lg:pr-2">{pages.map((page) => <button key={page.pageNumber} type="button" aria-current={currentPage === page.pageNumber ? 'page' : undefined} onClick={() => onSelect(page.pageNumber)} className={`h-24 w-28 shrink-0 overflow-hidden rounded-lg border bg-surface p-2 text-left lg:w-full ${currentPage === page.pageNumber ? 'border-accent ring-1 ring-accent' : 'border-border'}`}><span className="block text-[10px] font-semibold text-ink-muted">Page {page.pageNumber}</span><span className="mt-1 line-clamp-4 block text-[10px] leading-4 text-ink">{page.textPreview || 'Blank page'}</span></button>)}</nav>
}
