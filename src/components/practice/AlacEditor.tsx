import type{PracticeFinding}from'../../lib/practiceTypes'
type Parts={direct:string;legalBasis:string;application:string;conclusion:string}
export function AlacEditor({value,onChange,findings=[]}:{value:Parts;onChange:(v:Parts)=>void;findings?:PracticeFinding[]}){const entries:[keyof Parts,string,string][]=[['direct','Direct Answer','Yes, No, or the legal result…'],['legalBasis','Legal Basis','State the controlling rule or doctrine…'],['application','Application','Apply the material facts to the rule…'],['conclusion','Conclusion','State the resulting disposition…']];return <div className="space-y-4">{entries.map(([key,label,placeholder])=><label key={key} className="block text-sm font-medium">{label}<textarea spellCheck value={value[key]} placeholder={placeholder} onChange={e=>onChange({...value,[key]:e.target.value})} className="mt-1 min-h-32 w-full rounded-xl border border-border bg-surface p-4 font-normal"/></label>)}<div aria-live="polite" className="space-y-1">{findings.map((f,i)=><p key={i} className="text-sm text-amber-700">{f.message}</p>)}</div></div>}
