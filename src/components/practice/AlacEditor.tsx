import type{PracticeFinding}from'../../lib/practiceTypes'
type Parts={direct:string;legalBasis:string;application:string;conclusion:string}
export function AlacEditor({value,onChange,findings=[]}:{value:Parts;onChange:(v:Parts)=>void;findings?:PracticeFinding[]}){const entries:[keyof Parts,string,string,string][]=[['direct','1 · Direct answer','First sentence','Begin with Yes, No, or a response that directly answers the question.'],['legalBasis','2 · Legal basis','Second to third sentence','State the controlling law, rule, doctrine, or jurisprudence. Exact citations are helpful but not required.'],['application','3 · Apply the law','Third to fourth sentence','Connect the material facts to the controlling law.'],['conclusion','4 · Conclusion','Last sentence','Reiterate the legal conclusion or answer.']];return <div className="space-y-4"><div className="rounded-2xl border border-border bg-surface p-4"><h2 className="font-semibold">One question, one concise answer</h2><p className="mt-1 text-sm text-ink-muted">Use the four fields as sentence guidance. A2 combines them into one answer when you submit.</p></div>{entries.map(([key,label,timing,placeholder])=><label key={key} className="block text-sm font-medium"><span className="flex items-baseline justify-between gap-3"><span>{label}</span><span className="text-xs font-normal text-ink-muted">{timing}</span></span><textarea spellCheck value={value[key]} placeholder={placeholder} onChange={e=>onChange({...value,[key]:e.target.value})} className="mt-1 min-h-28 w-full rounded-xl border border-border bg-surface p-4 font-normal leading-7"/></label>)}<div aria-live="polite" className="space-y-1">{findings.map((f,i)=><p key={i} className="text-sm text-amber-700">{f.message}</p>)}</div></div>}
