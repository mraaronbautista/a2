import type{FactChange}from'../../lib/practiceTypes';export function VariationDiffView({changes}:{changes:FactChange[]}){return <div className="space-y-2">{changes.map((change,index)=><div key={index} className="rounded-xl border border-border bg-bg p-3"><p className="text-xs font-semibold uppercase text-ink-muted">{change.field}</p><div className="mt-2 grid gap-2 text-sm sm:grid-cols-2"><p><span className="text-ink-muted">Before: </span>{change.previousValue}</p><p><span className="text-ink-muted">After: </span>{change.newValue}</p></div><p className="mt-2 text-sm text-accent">Legal effect: {change.legalEffect}</p></div>)}</div>}
