import type{PracticeFinding}from'../../lib/practiceTypes';export function WritingFindingsPanel({findings}:{findings:PracticeFinding[]}){return <section aria-live="polite" className="rounded-2xl border border-border bg-surface p-4"><h3 className="font-semibold">Writing checks</h3><p className="mt-1 text-xs text-ink-muted">Simple mechanical checks only—not substantive legal feedback or full grammar analysis.</p>{findings.length?<ul className="mt-3 list-disc space-y-1 pl-5 text-sm">{findings.map((f,i)=><li key={i}>{f.message}</li>)}</ul>:<p className="mt-3 text-sm text-ink-muted">No mechanical issues detected.</p>}</section>}
